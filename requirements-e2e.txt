# Browser smoke suite (tests_e2e/), run by the "Browser smoke" CI job.
# After installing: `python -m playwright install --with-deps chromium`.
-r requirements.txt
pytest>=8.0,<10.0
playwright>=1.47,<2.0
